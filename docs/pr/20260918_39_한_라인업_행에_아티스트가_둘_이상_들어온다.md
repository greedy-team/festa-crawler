## ✨ 변경 사항

**합동 무대가 한 줄로 적힌 글을 아티스트별 행으로 나눕니다.** 라인업 행은 아티스트 1명 단위라는 전제가 깨져 있었습니다 — 묶인 문자열이 아티스트 마스터에 한 명으로 등록되고, 정규화가 확신하지 못해 `needs_review`로 남습니다.

- **`split_artists()` 신설** — 구분자는 **앞뒤 공백이 있는 `" & "` 하나뿐**입니다
- 나뉜 행은 같은 `day` 안에서 `order`를 이어 받고, 정규화 매핑도 **이름별로** 붙습니다
- 시크릿 게스트는 이름이 빈 값이라 나누지 않습니다

**쉼표와 괄호는 건드리지 않습니다.** 갈라서는 안 되는 표기가 실제로 있기 때문입니다 — `빵송국(곽범, 이창호, 이선민)`은 팀과 멤버, `카더가든(Car, the Garden)`은 한 아티스트의 영문 병기입니다. 둘 다 테스트로 고정했습니다.

**추출 프롬프트가 아니라 CSV를 만드는 단계에서 나눕니다.** 프롬프트를 고치면 추출 결과가 달라져 `SCHEMA_VERSION`을 올려야 하고, 전 연도 재수집(74행)이 따라옵니다. 캐시된 추출 결과를 옮기는 지점에서 나누면 **재수집 없이** 적용됩니다.

## ✅ 테스트

- [x] 수동 테스트 완료
- [x] 테스트 코드 완료

**217 → 222 passed.** 신규 5건 중 3건은 구현 전 RED를 확인했습니다(나머지 2건은 "나누지 않는다"를 고정하는 가드라 처음부터 초록).

2026 재생성 실측 — `Keyveatz & KiiiKiii`가 두 행으로 갈리고 라인업 253 → 254행, `" & "` 잔존 0건. LLM 호출 0건(캐시 히트).

## 🔎 봐 주셨으면 하는 곳

- **구분자를 `" & "` 하나로 좁힌 판단.** `&`를 공백 없이 쓰는 표기나 `feat.`, `X` 같은 협업 표기는 그대로 둡니다. 실데이터에서 오탐이 더 비싸다고 봤습니다
- 지난 연도(2023~2025)에는 아직 적용되지 않습니다 — 그 연도를 다시 돌리면 캐시 스키마가 달라 재추출이 함께 걸립니다

관련 이슈: #39

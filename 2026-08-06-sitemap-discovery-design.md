# 사이트맵 기반 후보 탐색 설계 (v1)

작성일: 2026-08-06 · 이슈 #11

후보 URL 탐색에 사이트맵 경로를 추가해 웹 검색 호출을 줄이는 설계다.
기존 탐색(`discover.py`)을 대체하지 않고 그 **앞에** 놓는다.

## 1. 배경

탐색은 전적으로 `claude` CLI의 WebSearch에 의존한다. 실측 1건 60초 이상이고 구독 세션 한도를
소모하는데, 이 한도는 `AGENTS.md`에 운영 중단 원인으로 명시돼 있다.

지금 시즌은 시드에 URL이 21곳 채워져 있어 탐색이 9곳만 돌았다. **문제는 다음 시즌이다.**
시드 URL 21개 중 19개가 주소에 연도가 박힌 특정 연도 글이라(`.../entry/2026-서울대학교-축제...`),
연도가 2027로 바뀌면 전부 `mismatch`로 떨어지고 29곳 전부가 탐색으로 넘어간다.
29곳 × 60초면 30분에 가깝고, 중간에 세션 한도에 걸릴 가능성이 높다.

한편 우리가 이미 출처로 쓰는 블로그들은 사이트맵을 열어두고 있다. 티스토리 계열은 글 주소에
제목이 슬러그로 박히므로 **주소만 보고 대학명·연도를 판별할 수 있다.**

## 2. 조사 결과 (2026-08-05~06 실측)

**블로그 검색 기능은 쓸 수 없다.** `memogipost.tistory.com`·`news.comingmoney.com`·
`jcks100.com` 모두 `robots.txt`에 `Disallow: /search`, `/m/search`를 명시한다. 구글·네이버
검색 결과 페이지를 긁지 못하는 것과 같은 근거이며 우회는 하드 제약 위반이다.

**사이트맵은 차단 목록에 없다.** 대상 5곳에서 글 URL 5260건을 얻었다.

| 사이트맵 | 글 수 |
|---|---|
| `memogipost.tistory.com` | 2112 |
| `jcks100.com` | 1617 |
| `news.comingmoney.com` | 667 |
| `schedule.comingmoney.com` | 479 |
| `towbworld.tistory.com` | 420 |

**매칭 실측:**

| 규칙 | 커버 | 문제 |
|---|---|---|
| 정식 표기 (`고려대학교`) | 17/29곳 | 없음 |
| 어간 (`고려대`) | 23/29곳 | 추가 21건 중 대부분이 다른 캠퍼스 (고려대 세종, 한양대 에리카, 건국대 글로컬, 중앙대 다빈치, 연세대 미래) |
| 줄임말 (`고대`·`성대`) | +0곳 | 부분 문자열 충돌 — `성대`가 경성대·한성대를, `연대`가 경**연대**회를, `한대`가 유**한대**학교를 잡는다 |

줄임말로만 실린 시드 대학은 사실상 없다. `이화여대`·`덕성여대` 글이 따로 있으나 **둘 다 정식
표기 글이 이미 있어** 줄임말 없이도 잡힌다. 유일한 예외 `한국외대`는 글로벌캠퍼스 글이라
잡아도 틀린 출처다.

**커버리지는 늘지 않는다.** 현재 실패 중인 3곳(숙명여대·동덕여대·삼육대)은 5개 블로그 어디에도
없다. 웹에 아직 정보가 없는 것이며, 이 작업의 목적이 아니다.

## 3. 목적과 범위

**목적은 탐색 비용 절감이다.** 커버리지 확대가 아니다.

| | WebSearch만 | 사이트맵 도입 후 |
|---|---|---|
| 사이트맵이 커버 | — | 17곳 (LLM 0콜) |
| WebSearch 필요 | 29곳 × 60초 ≈ 29분 | 12곳 × 60초 ≈ 12분 |

**만드는 것**

- `fetch.fetch_text()` — robots·간격을 지키는 텍스트 수집
- `discover.discover_sitemap()` — 사이트맵 수집 + 엄격 매칭
- `crawl.process_row`의 탐색 순서 재구성 (WebSearch 지연 호출)
- `discovery` 컬럼에 `sitemap` 값 추가

**안 만드는 것**

교차 검증(파이프라인 문서 4.3), 줄임말·어간 매칭, 캠퍼스 판정, `verify` 확장.
사이트맵 출처의 동적 추가. 전부 별건이다.

## 4. 흐름

각 소스가 자기 상한을 갖고, 앞 소스가 실패해야 다음이 돈다.

```
시드 URL 있음 → 시도 → ok면 discovery=manual
    ↓ 실패 또는 URL 없음
사이트맵 후보 (엄격 매칭, 최대 MAX_CANDIDATES) → 순서대로 시도 → ok면 discovery=sitemap
    ↓ 전부 verify 실패
WebSearch 후보 (기존 discover_cached, 최대 MAX_CANDIDATES) → 시도 → ok면 discovery=search
    ↓ 전부 실패
no_candidate (수동 URL이 있었다면 그 실패 사유를 유지)
```

**WebSearch는 지연 호출이다.** 사이트맵 후보가 `verify`를 통과하면 `discover_cached`를
**아예 부르지 않는다.** `output/discovered/<대학>.json`도 생기지 않는다 — 이것이 절감의 실체다.

기존 `discover_cached`의 반환 규약(후보 목록 / 탐색 실패 시 `None`)은 바꾸지 않는다.
`None`이면 지금처럼 결과를 캐시하지 않고 다음 실행에서 재시도한다.

## 5. 매칭 규칙

사이트맵 XML에서 `<loc>`를 뽑아 퍼센트 디코딩한 뒤, 아래를 **모두** 만족하는 것만 후보로 삼는다.

1. `/entry/`를 포함한다 (글 URL. `/category/` 등 목록 페이지 제외)
2. **대학 정식 표기가 그대로 들어 있다** (`고려대학교`). 어간·줄임말 매칭은 하지 않는다
3. 대상 연도 문자열이 들어 있다 (`2026`)
4. 기존 `discover._is_blocked()`를 통과한다 (차단 도메인 방어선 재사용)

2번의 "정식 표기"는 `universities.csv`의 `university` 값 그대로이고, 3번의 연도는 같은 행의
`year`를 문자열로 쓴다. 상수로 박지 않는다.

정렬은 사이트맵 등장순이다. 관련성 점수를 매기지 않는다 — 수집·탐색 모듈은 판단하지 않고
판정은 `verify`가 전담한다는 기존 결정(볼트 DEC-0017)을 그대로 따른다.
`서울대공원` 같은 오탐은 `verify`가 대학명 불일치로 걸러낸다.

## 6. 구현

### 6.1 `fetch.py` — `fetch_text(url) -> str | None`

`_robots_allowed()`·`_respect_rate_limit()`·`USER_AGENT`·`TIMEOUT_SECONDS`를 그대로 재사용해
응답 본문을 문자열로 돌려준다. HTML 파싱은 하지 않는다. 실패(robots 차단·네트워크·HTTP 오류)
시 `None`.

네트워크 정책을 `fetch.py` 한 곳에 유지하기 위해서다. robots·간격 규칙이 두 모듈에 생기면
반드시 어긋난다.

### 6.2 `discover.py`

```python
SITEMAP_SOURCES = (...)      # 5곳 sitemap.xml URL 상수
_SITEMAP_URLS = None         # 프로세스 1회 로드 (메모리 캐시)

def discover_sitemap(university: str, year: int) -> list[str]
```

**사이트맵은 디스크에 캐시하지 않는다.** 새 글이 계속 올라와 재실행하면 달라지는 상태이기
때문이다 — `LSN-0005`가 규정한 성질이다. 프로세스 안에서만 재사용해 29곳을 순회해도 요청은
5건으로 유지한다.

소스 하나가 실패하면 그 소스만 건너뛰고 나머지로 진행한다. 전부 실패하면 빈 목록을 반환하고
호출자가 WebSearch로 넘어간다.

### 6.3 `crawl.py`

후보 시도 루프를 헬퍼로 뽑는다. 지금 `process_row` 안에 인라인으로 있는 블록을 그대로 복사해
두 번 쓰지 않기 위해서다.

```python
def _try_candidates(candidates, row, record, source) -> bool
```

`process_row`는 사이트맵 → WebSearch 순으로 이 헬퍼를 부른다. `discovery` 값은 헬퍼의
`source` 인자가 그대로 기록한다.

### 6.4 산출물

`festivals.csv`의 `discovery` ∈ `manual | sitemap | search | ""`.
컬럼 구성은 바뀌지 않는다. README의 해당 설명을 갱신한다.

## 7. 편중 감시

볼트에 "단일 블로거 의존 67% → 기관으로 분산"이 2026-08-05의 성과로 기록돼 있다.
사이트맵을 탐색 앞단에 두면 그 성과가 역행할 수 있다. 대상 5곳이 전부 개인·소규모 블로그이기
때문이다.

**지금 추측으로 순서를 정하지 않는다.** `discovery` 컬럼이 경로를 기록하므로 시즌 종료 후
"사이트맵 출처가 몇 %였고 그 정확도가 어땠나"를 셀 수 있다(파이프라인 문서 5.2의 피드백 루프).
수치가 나쁘면 그때 순서를 바꾸거나 기관 도메인 우선 규칙을 검토한다.

## 8. 에러 처리

- **사이트맵 소스 일부 실패** — 그 소스만 건너뛴다. 남은 소스로 진행
- **전부 실패** — 후보 0건. WebSearch로 폴백한다. 일시적 실패를 굳히지 않는다
  (메모리 캐시뿐이라 다음 실행에서 자동 재시도)
- **XML 파싱 실패** — 해당 소스를 빈 목록으로 취급. 예외를 밖으로 던지지 않는다
- **robots 차단** — `fetch_text`가 `None`을 반환하고 같은 경로로 처리된다

## 9. 테스트와 완료 기준

네트워크는 전부 monkeypatch한다. 테스트가 실제 요청이나 LLM 호출을 하면 안 된다.

| 대상 | 확인 내용 |
|---|---|
| 엄격 매칭 | `2026-고려대학교-대동제`는 잡고, `2026-고려대-과학기술대학-대동제`·`2026-서울대공원-장미원축제`는 잡지 않는다 |
| 연도 필터 | 다른 연도 글 제외 |
| 경로 필터 | `/category/...` 등 비-글 URL 제외 |
| 차단 도메인 | `_is_blocked` 적용 |
| 메모리 캐시 | 서로 다른 두 대학을 연달아 조회해도 사이트맵 fetch는 1회 |
| 소스 실패 | 5곳 중 일부가 `None`이어도 예외 없이 나머지로 진행 |
| 전부 실패 | 빈 목록 반환 |
| **지연 호출** | 사이트맵 후보가 성공하면 `discover_cached`를 부르지 않는다 |
| 폴백 | 사이트맵 후보가 전부 verify 실패하면 WebSearch로 넘어간다 |
| `discovery` 기록 | 채택 경로에 따라 `manual`/`sitemap`/`search`가 정확히 기록된다 |

**완료 기준**

- `pytest` 전체 통과 (기존 95개 + 신규)
- 캐시를 비운 뒤 `--limit`으로 일부 대학만 실제 실행 → `discovery=sitemap` 행이 나오고,
  그 행의 `source_url`이 해당 대학·해당 연도 글임을 눈으로 확인
- `requirements.txt`·`version.yml` 무변경

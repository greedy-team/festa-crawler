# 데이터 파이프라인 v1

### 블로그 데이터 존재 여부

[서울 상위 대학 29개](https://app.notion.com/p/29-3abc1da29c068055b102f0d7dac02863?pvs=21)

---

## 1. 배경과 문제 정의

대학 축제 라인업 정보는 수요는 명확한데 공급이 파편화되어 있다. 각 대학 총학생회 SNS, 개인 블로그, 커뮤니티에 흩어져 있고 형식도 제각각이다. 한곳에 모아 정규화된 형태로 제공하는 것이 이 서비스의 핵심 가치다.

문제는 **원천 데이터를 어떻게 확보하느냐**다. 아래 순서로 검토했고 결론은 마지막 항목이다.

| 후보 | 가능 여부 | 판단 |
| --- | --- | --- |
| Instagram oEmbed | 불가 | URL을 이미 아는 게시물의 임베드용. 검색 기능 자체가 없음 |
| Instagram Graph API | 불가 | 자기 소유 Business/Creator 계정 미디어만 조회 가능 |
| 티스토리 Open API | 불가 | 2024년 2월 단계적 종료 |
| OG 메타태그 파싱 | 부분적 | 제목/썸네일만 확보. 라인업 본문은 못 가져옴 |
| **본문 크롤링 + 수동 입력 병행** | **채택** | 유일하게 실효성 있는 조합 |

## 2. 법적 검토

### 2.1 저작권

저작권은 표현을 보호하지 사실을 보호하지 않는다. 아티스트명, 공연 일자, 시간표는 순수 사실 정보이므로 그 자체는 저작물이 아니다. 블로그 글의 문장 표현을 복제하는 것이 아니라 사실 데이터만 추출하는 행위는 원칙적으로 저작권 침해에 해당하지 않는다.

덧붙여 라인업 정보의 원출처는 대개 각 대학 축제 준비위원회이며, 블로그 글 자체가 이미 2차적으로 정리한 결과물인 경우가 많다.

### 2.2 남아 있는 리스크

- **데이터베이스제작자의 권리** — 체계적으로 구축된 대규모 DB는 별도 보호 대상이 될 수 있으나, 개별 블로그 글 한 편은 요건(상당한 투자에 의한 체계적 배열)에 해당할 가능성이 낮다.
- **이용약관 위반** — 저작권과 별개 문제. 다만 이는 플랫폼과 블로거 간 계약이므로 제3자에게 직접 계약 책임을 묻기는 어렵고, 현실적 리스크는 IP 차단 수준이다.
- **부정경쟁방지법상 데이터 부정사용** — 2022년 개정으로 신설되었으나 상업적 대규모 데이터셋을 겨냥한 조항이라 본 건에 적용될 가능성은 낮다.

### 2.3 준수 사항

- robots.txt 명시 차단 경로 접근 금지 (`/guestbook`, `/manage`, `/owner`, `/admin`, `/search` 등)
- 동일 호스트 요청 간 최소 3초 간격 유지
- 원문 전재 금지, 사실 데이터만 추출
- 출처 링크 표기

> 본 항목은 일반적 원칙 정리이며 법률 자문이 아니다. 서비스를 상업화하거나 규모를 키울 경우 별도 검토가 필요하다.
> 

## 3. 아키텍처

### 3.1 두 개의 수집 경로

```
[크롤링 수집] ─┐
               ├─→ [통합 정규화] → [검수 큐] → [발행]
[수동 입력]  ─┘
```

![image.png](%EB%8D%B0%EC%9D%B4%ED%84%B0%20%ED%8C%8C%EC%9D%B4%ED%94%84%EB%9D%BC%EC%9D%B8%20v1/image.png)

크롤링은 **초안 생성기**이고, 최종 신뢰는 사람이 담보한다. 이 역할 분담이 전체 설계의 축이다.

| 경로 | 강점 | 약점 | 역할 |
| --- | --- | --- | --- |
| 크롤링 | 커버리지, 속도 | 정확도 보증 불가 | 초안 대량 생산 |
| 수동 입력 | 정확도 | 인력 소요 | 최종 확정, 크롤링 실패 보완 |

### 3.2 크롤링 경로 4단계

**1단계 — 탐색**
대학명과 연도로 검색해 후보 URL을 수집한다. 실패 모드는 축제와 무관한 글이나 다른 연도 글의 혼입이다. 측정 지표는 후보 중 실제 라인업 글의 비율(적중률).

**2단계 — 수집**
URL에서 본문 텍스트를 확보한다. 티스토리는 스킨마다 본문 컨테이너 클래스가 다르므로 복수 셀렉터를 순차 시도하고, 실패 시 범용 본문 추출기로 폴백한다. 실패 모드는 셀렉터 미스매치, 그리고 라인업이 포스터 이미지로만 게시된 경우다.

**3단계 — 추출**
본문 텍스트를 LLM에 전달해 구조화된 JSON으로 변환한다. 자유 텍스트 응답 대신 도구 호출을 강제해 파싱 실패를 원천 차단한다. 실패 모드는 존재하지 않는 아티스트 생성(환각), 일차·날짜 매핑 오류.

**4단계 — 적재**
정규화 후 저장한다. 실패 모드는 동일 축제 중복 레코드, 아티스트명 표기 불일치.

### 3.3 설계상 가장 어려운 지점

직관과 달리 3단계(추출)가 아니라 **1단계(탐색)가 병목**이다. 추출은 본문만 제대로 들어오면 LLM이 안정적으로 처리한다. 반면 탐색은 "이 글이 정말 그 대학, 그 연도의 라인업인가"를 검색 결과 제목만으로 판단해야 한다.

해결책은 역방향 검증이다. 3단계 결과물에 `university`, `year`, `found` 필드를 포함시켜, 추출 결과로 1단계 후보의 타당성을 사후 판정한다.

## 4. 데이터 모델

### 4.1 라인업 레코드

핵심 필드:

- `university_name` — 대학명
- `university_id` — 대학 ID
- `festival_name` — 축제명
- `year` — 연도
- `lineup[]` — 아티스트 목록
    - `artist` — 아티스트/팀명 (필수)
    - `day_label` — 원문 표기 그대로 (예: 1일차, DAY1)
    - `date` — 정규화 가능하면 YYYY-MM-DD
    - `time` — 공연 시각

---

- `festival_name` — 축제명
- `festival_id` — 축제 ID
- `organizer_id` — 주최 기관 ID
    - `organizer_name` — 주최 기관명
    - `festival_type` — `UNIVERSITY` / `LOCAL` / `MUSIC` / `CULTURE` / `OTHER`
- `year` — 연도
- `region` — 지역
- `venue_name` — 개최 장소
- `start_date` — 시작일
- `end_date` — 종료일
- `lineup[]` — 아티스트 목록
    - `artist` — 아티스트·팀명
    - `day_label` — 원문 표기 그대로
    - `date` — 정규화 가능하면 `YYYY-MM-DD`
    - `time` — 공연 시각

### 4.2 출처 메타데이터

하이브리드 구조에서 반드시 필요한 필드:

- `source_type` — `CRAWLED` / `MANUAL`
- `status` — `PENDING_REVIEW` / `APPROVED` / `REJECTED`
- `source_url` — 크롤링 시 원본 링크. 검수자가 대조할 수 있어야 하므로 필수
- `reviewed_by`, `reviewed_at` — 승인 이력
- `confidence` — 서로 다른 출처에서 교차 확인된 횟수

---

### 4.3 신뢰도 산정

같은 축제에 대해 복수의 블로그 글이 검색되는 것은 노이즈가 아니라 **기회**다. 서로 다른 출처 3곳에서 동일 아티스트가 나오면 신뢰도 상, 1곳에서만 나오면 하로 판정한다. 단일 블로그의 오타나 LLM 환각을 걸러내는 가장 저렴한 수단이다.

## 5. 운영 정책

### 5.1 발행 규칙

- 크롤링 데이터는 **절대 자동 발행하지 않는다.** 무조건 `PENDING_REVIEW`로 검수 큐에 적재
- 수동 입력만 즉시 발행 가능
- 근거: 축제 라인업은 틀리면 사용자가 즉시 인지하는 데이터다. 정확도가 곧 서비스 신뢰도이며, "이 아티스트가 실제로 오는가"는 기계가 보증할 수 없는 영역이다

### 5.2 충돌 처리

동일 축제에 크롤링 데이터와 수동 데이터가 공존할 경우 **수동이 무조건 우선**한다.

단, 크롤링 데이터를 삭제하지 않고 비활성 상태로 보존한다. 이는 "크롤링이 얼마나 정확했는가"를 측정하는 정답지가 되며, 파이프라인 개선의 유일한 피드백 루프다.

### 5.3 검수 화면 요건

검수가 병목이 되면 파이프라인 전체가 정지한다. UX 승부처.

- 원본 블로그와 추출 결과를 **한 화면에 나란히** 배치
- 승인 / 수정 / 반려를 키보드로 처리
- 목표 처리 시간 건당 10초

필드별로 폼을 일일이 채우게 만들면 건당 2분이 소요되고, 결국 아무도 사용하지 않게 된다.

## 6. 로드맵

### 6.1 구축 순서

| 단계 | 범위 | 목적 |
| --- | --- | --- |
| 1주차 | URL 직접 입력 → 수집·추출만 검증 | 본문 추출 성공률과 LLM 정확도를 먼저 확정 |
| 2주차 | 탐색 자동화 연결 | 후보 URL 적중률 측정 |
| 3주차 | 교차 검증, 정규화, 저장 | 중복 병합과 신뢰도 산정 |
| 4주차 | 검수 화면 | 운영 가능 상태 진입 |

탐색부터 만들면 결과가 이상할 때 검색 문제인지 추출 문제인지 분리되지 않아 디버깅 비용이 급증한다. **반드시 뒷단부터 검증한다.**

### 6.2 출시 전략 재검토

대학 50곳 기준 축제 시즌은 5월과 9월에 집중된다. 시즌당 50건 검수는 건당 10초 기준 10분 이내로, 1인이 충분히 감당 가능한 규모다.

이는 곧 **이 규모에서 크롤링 자동화의 한계 이득이 크지 않을 수 있음**을 의미한다. 1차 버전을 수동 입력만으로 출시하고 크롤링을 2차로 붙이는 방안을 진지하게 검토할 가치가 있다.

> 크롤링 없이도 서비스는 성립한다. 그러나 검수 없는 크롤링만으로는 성립하지 않는다.
> 

## 7. 비용 관리

LLM 호출 지점이 탐색과 추출 두 군데이므로 비용은 `대학 수 × 후보 URL 수`로 증가한다. 대학 50곳에 후보 8건이면 400회 호출이다.

필수 대응:

- 동일 URL 재호출 차단 (캐싱)
- 본문 길이 절단 (상한 설정)
- 탐색 결과 파일 저장 후 재실행 시 재사용

## 8. 리스크 정리

| 리스크 | 영향 | 대응 |
| --- | --- | --- |
| 라인업이 포스터 이미지로만 게시 | 텍스트 추출 불가 | 이미지 URL 추출 후 비전 모델 처리 (2차 확장) |
| LLM 환각으로 허위 아티스트 생성 | 서비스 신뢰도 직결 | 검수 필수화 + 교차 검증 |
| 티스토리 스킨 변경으로 셀렉터 무효화 | 수집 실패율 상승 | 범용 추출기 폴백 + 실패율 모니터링 |
| 검수 병목 | 파이프라인 정지 | 검수 UX 최적화, 수동 입력 경로 상시 개방 |
| 원본 블로그 사후 수정 | 데이터 불일치 | 발행 후 재검증 주기 설정 (2차 확장) |
| IP 차단 | 수집 중단 | 요청 간격 준수, User-Agent 명시 |

## 9. 향후 확장 후보

- 포스터 이미지 OCR 분기 — 텍스트 추출 실패 건을 비전 모델로 재처리
- 매칭 키 설계 — 크롤링과 수동 데이터를 동일 축제로 판정하는 기준 정립
- 발행 후 변경 감지 — 원본 수정 시 재검수 트리거
- 커버리지 대시보드 — 대학별 수집 현황과 크롤링 정확도 추이
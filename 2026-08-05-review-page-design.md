# 검수 페이지 설계 (v1)

작성일: 2026-08-05

크롤러 산출물(`output/*.csv`)을 브라우저에서 훑고, 문제 있는 행만 골라 다시 돌리는
**로컬 전용 읽기 전용 뷰어**의 설계다. 배포 대상이 아니며, `crawl.py`처럼 손으로 띄운다.

## 1. 배경

크롤러는 초안 생성기이고 최종 신뢰는 사람이 담보한다. 지금은 그 검수를 CSV를 엑셀로 열어
한다 — `flag != ok` 행을 찾고, `source_url`을 복사해 브라우저에 붙이고, 추출 결과와 원본을
번갈아 보는 흐름이다. 축제 29곳·라인업 219행 규모에서 이미 번거롭고, 대상 대학이 늘면
검수가 파이프라인 전체의 병목이 된다 (파이프라인 문서 5.3).

이 페이지는 그 훑는 단계를 대체한다. **값을 고치는 단계는 대체하지 않는다** — 수정은 지금처럼
어드민 입력에서 한다.

## 2. 범위

**만드는 것**

- `serve.py` — `http.server` 기반 로컬 서버. 외부 의존성 추가 없음
- `review.html` — 검수 화면 한 장. 바닐라 JS, 빌드 도구 없음
- `festival_id` 도입에 따른 `crawl.py`·`enrich.py`·테스트 수정

**안 만드는 것**

값 수정·저장, 인증, 배포, 빌드 파이프라인, 브라우저 E2E 테스트. 페이지는 읽기 전용이고
`output/`에 쓰는 주체는 여전히 `crawl.py`와 `enrich.py`뿐이다.

**사용자**는 크롤러 개발자 1인이다. venv와 `claude` 로그인이 이미 갖춰진 환경을 전제하며,
설치 안내나 환경 진단은 넣지 않는다.

## 3. 스키마 정정 — `festival_id`

### 3.1 무엇이 어긋나 있었나

파이프라인 문서 4.1(개정판)의 축제 레코드는 `festival_id`·`organizer_id`·`organizer_name`·
`festival_type`을 갖고 `lineup[]`을 그 안에 품는다. 크롤러는 이 중 어느 것도 갖고 있지 않고,
평평한 CSV 두 장을 `university + year` 자연키로 조인한다.

`2026-08-04-crawler-pipeline-design.md` §6은 문서 4.2(출처 메타데이터)를 "DB 적재 단계 소관"
이라며 명시적으로 제외했지만, **4.1의 식별자에 대해서는 아무 말도 하지 않았다.** 문서화되지
않은 이탈이다.

이탈이 만드는 실제 문제는 두 가지다.

- 대학 아닌 축제(`LOCAL`/`MUSIC`/`CULTURE`)로 넓힐 때 스키마가 대학 전용이라 맞지 않는다
- 조인 키가 대학명 문자열이라, 표기가 바뀌면 조인이 오류 없이 조용히 깨진다

### 3.2 결정: `festival_id`만 도입한다

`festival_id = "<university>-<year>"`, 한글 그대로 (`건국대학교-2026`).

| 파일 | 변경 |
|---|---|
| `festivals.csv` | `festival_id`를 첫 컬럼으로 추가 |
| `lineup.csv` | `festival_id` 추가, `university`·`year`·`festival_name` 제거 (10 → 8 컬럼) |
| `artists.csv` | 변경 없음 |

근거:

- 시드 29행에서 `university + year` 조합이 유일함을 확인했다 (중복 0건). 캠퍼스가 다른 대학도
  현재 시드에는 없다. 향후 같은 대학의 복수 캠퍼스가 들어오면 그때 `campus`를 id에 넣는다
- 한글을 그대로 쓰는 이유는 `output/raw/건국대학교.json`이 이미 한글 파일명을 쓰고 있어
  새 관례가 아니기 때문이다. 로마자 슬러그는 시드에 영문명 컬럼이 없어 29개를 손으로
  채워야 하는데, 사람이 읽는 초안 CSV에 그만한 값어치가 없다
- `enrich.py`는 `crawl.LINEUP_FIELDS`를 import하므로 상수 한 곳만 고치면 따라온다.
  `collect_raw_names`는 `output/raw/*.json`을 읽으므로 영향 없다

### 3.3 보류: `organizer_id`·`organizer_name`·`festival_type`

전 행이 `UNIVERSITY` 상수이고, `organizer_id`가 가리킬 주최 기관 마스터가 존재하지 않는다.
대학 아닌 축제가 실제로 들어올 때 추가한다 — 그때 `festival_type`이 비로소 변수가 된다.

## 4. 아키텍처

`serve.py` 하나가 정적 파일 서빙·JSON API·서브프로세스 관리를 겸한다.
`ThreadingHTTPServer`를 쓰고, `127.0.0.1`에만 바인드한다. 기본 포트 8765(`--port`로 변경),
기동 시 `webbrowser.open`으로 브라우저를 연다.

### 4.1 엔드포인트

| 엔드포인트 | 하는 일 |
|---|---|
| `GET /` | `review.html` 반환 |
| `GET /api/data` | `output/`의 CSV 3종을 읽어 `{festivals, lineup, artists}` JSON으로 |
| `POST /api/run` | `{job, university?, rediscover?}` — 잡 시작. 실행 중이면 `409` |
| `GET /api/log?from=N` | `N`번째 이후 로그 줄 + `running` 여부 |

`/api/data`는 요청마다 파일을 다시 읽는다. 29·219·121행 규모라 캐시할 이유가 없고,
캐시를 두면 crawl이 CSV를 갱신했을 때 무효화 시점을 관리해야 한다.

### 4.2 실행

`subprocess.Popen([sys.executable, "crawl.py"], cwd=<repo>)`로 띄운다. `sys.executable`을 쓰면
서버를 띄운 venv의 파이썬이 그대로 이어지므로 인터프리터 경로를 따로 받지 않는다.

백그라운드 스레드가 자식 stdout을 줄 단위로 읽어 메모리 리스트에 쌓고, 브라우저는
`/api/log?from=N`을 1초 간격으로 폴링한다. SSE도 가능하지만 재연결·하트비트 처리가 붙는데,
혼자 쓰는 로컬 페이지에 그 값어치가 없다.

**동시 실행은 전역 잡 슬롯 하나로 막는다.** 실행 중이면 `409`. LLM 세션 한도를 공유하는
프로세스를 둘 띄우면 양쪽이 함께 깨진다.

### 4.3 행 단위 재실행 — 캐시 삭제로 한다

**행 단위 실행을 위한 `crawl.py` 변경은 없다** (§3의 `festival_id` 변경과는 별개다).
해당 대학의 캐시 파일을 지우고 `crawl.py`를 통째로 실행하면,
나머지 28곳은 캐시 히트로 즉시 지나가고 그 한 곳만 실제로 처리되며 CSV 재생성까지 따라온다.

| 버튼 | 지우는 파일 | 대상 |
|---|---|---|
| 다시 돌리기 | `output/raw/<대학>.json` | `mismatch`·`empty_body` 등 출처는 맞고 추출이 틀린 행 |
| 탐색부터 | 위 + `output/discovered/<대학>.json` | `no_candidate` — 후보 목록 자체가 쓸모없던 행 |

버튼을 둘로 나눈 이유는 README에 이미 문서화된 두 절차와 1:1로 대응하기 때문이다. 서버가
`flag`를 보고 자동으로 고르면 숨은 로직이 되고, 왜 탐색에 60초를 더 썼는지 설명할 수 없게 된다.

삭제는 `missing_ok=True`다. 버튼을 두 번 눌러도 아무 일이 없어야 한다.

### 4.4 신뢰 경계

`university` 파라미터가 파일 경로 조립에 쓰이므로, **`universities.csv`에 실제로 존재하는
대학명만 허용한다(allowlist).** 목록에 없으면 `400`을 반환하고 경로 조립 자체에 도달하지
않는다. 정규화나 이스케이프로 막지 않는다 — 허용 목록이 더 짧고 더 확실하다.

서버는 `127.0.0.1`에만 바인드한다. 로컬 전용이라도 임의 문자열로 경로를 만들게 두지 않는다.

## 5. 화면

파이프라인 문서 5.3의 검수 화면 요건(승인/반려 키보드 처리, 건당 10초)은 **이 페이지의
요건이 아니다.** 5.3은 DB 적재 후 어드민 검수 큐(`PENDING_REVIEW` → `APPROVED`/`REJECTED`)를
가리키며, 승인 상태는 CSV에 존재하지 않는다. 다만 5.3이 짚은 핵심 하나 — 원본과 추출 결과를
한 화면에 나란히 — 는 읽기 전용에서도 유효하므로 가져온다.

```
┌────────────────────────────────────────────────────────┐
│ [전체 크롤] [enrich]   ● 대기 중        로그 ▾          │
├──────────────────────┬─────────────────────────────────┤
│ 축제 29              │ 건국대학교-2026                  │
│ [문제만] [검색출처]   │  축제명 / 기간 / 장소 / 외부인   │
│ [전체]  [아티스트]    │  인스타 / 포스터 / discovery     │
│                      ├─────────────────────────────────┤
│ ● no_candidate 숙명   │ 라인업 8건                       │
│ ● no_candidate 동덕   │  10CM  ← 십센치   1일차 19:00   │
│ ○ ok      건국       │  ...                            │
│ ...                  ├─────────────────────────────────┤
│                      │ 원본 (iframe)   [새 탭 ↗]        │
└──────────────────────┴─────────────────────────────────┘
                         [다시 돌리기] [탐색부터]
```

**기본 화면이 봐야 할 것부터 보여준다.** 사람이 실제로 확인해야 하는 행은 세 종류다.

- `flag != ok` — 수집·추출·검증 중 하나가 실패
- `discovery = search` — 손으로 고른 출처가 아니므로 우선 확인 (README 운영 팁)
- `needs_review = true` — 아티스트 정규화 확신이 낮음

필터 칩이 이 셋에 대응하고, 진입 시 `문제만`이 켜져 있다.

**원본 표시.** `source_url`을 iframe으로 띄우고, `새 탭 ↗` 링크를 항상 함께 둔다.
티스토리 글은 `X-Frame-Options`·CSP가 없어 임베드되는 것을 확인했으나(2026-08-05 실측),
네이버 블로그 등은 막을 수 있다. 도메인별 판별 로직은 넣지 않는다 — 비어 보이면 링크를 누른다.

**키보드는 `j`/`k`(행 이동)만.** 읽기 전용이라 승인·반려 키가 없고, 목록 이동만으로 훑는
속도가 난다.

**아티스트는 별도 탭.** `artists.csv`를 `needs_review` 우선으로 보여준다. 축제와 조인해서 볼
이유가 없어 섞지 않는다.

## 6. 에러 처리

**빈 상태는 정상이다.** `output/`에 CSV가 아직 없거나, `crawl`만 돌리고 `enrich`를 안 해
`artists.csv`가 없는 것은 흔한 상태다. 해당 표를 "아직 없음"으로 두고 실행 버튼은 살려둔다.
서버가 500을 내는 것은 진짜 고장일 때뿐이다.

**실패는 남기고 자동 재시도하지 않는다.** 서브프로세스가 죽으면 종료 코드와 마지막 로그 줄을
화면에 그대로 둔다. 크롤러가 죽는 주된 원인은 Claude 세션 한도이며, 이는 시간이 지나야
풀리므로 자동 재시도가 상황을 악화시킨다. `LSN-0005`(재실행하면 달라질 상태는 캐시하지
않는다)와 같은 성질의 판단이다.

**crawl이 CSV를 쓰는 도중 읽을 수 있다.** 폴링이 파싱에 실패하면 그 회차만 건너뛰고 다음
폴링을 기다린다. 서버에 이전 스냅샷을 보관하지 않는다 — 1초 뒤 다시 읽으면 되는 일에 상태를
만들지 않는다.

## 7. 테스트와 완료 기준

혼자 쓰는 로컬 뷰어이므로 브라우저 E2E는 만들지 않는다. 대신 셋은 반드시 남긴다.

| 대상 | 확인 내용 |
|---|---|
| `festival_id` 스키마 | `festivals`↔`lineup`이 `festival_id`로 조인되는지, `lineup`에서 3개 컬럼이 제거됐는지. 기존 테스트 70개도 통과해야 한다 |
| 대학명 allowlist | `../../etc/passwd`·`건국대학교/../..` 같은 값이 `400`이며 **파일 경로 조립에 도달하지 않는지**. 신뢰 경계라 생략하지 않는다 |
| 캐시 삭제 | `다시 돌리기`는 `raw/`만, `탐색부터`는 `raw/`+`discovered/`를 지우는지. 파일이 없어도 예외가 나지 않는지 |

`/api/data`는 CSV 3종이 모두 없을 때와 일부만 있을 때를 포함해 확인한다.
`subprocess.Popen`은 monkeypatch로 차단한다 — 테스트가 실제 LLM을 호출해서는 안 된다.

**완료 기준**: `pytest` 전체 통과 + `serve.py`를 띄워 29행이 표시되고, `no_candidate` 행에서
`탐색부터`를 눌러 실제 재탐색이 도는 것까지 눈으로 확인.

## 8. 이 스펙에서 뺀 것 — 사이트맵 기반 탐색

브레인스토밍 중 별개 주제가 나왔고, 공유 코드가 없어 별도 스펙으로 분리했다. 재조사 비용을
줄이기 위해 조사 결과만 남긴다.

**블로그 사이트 검색은 쓸 수 없다.** `memogipost.tistory.com`·`news.comingmoney.com`·
`jcks100.com` 모두 `robots.txt`에 `Disallow: /search`, `/m/search`를 명시한다(2026-08-05 실측).
구글·네이버 검색 페이지를 긁지 못하는 것과 같은 근거이며, 우회는 하드 제약 위반이다.

**사이트맵은 막혀 있지 않다.** `memogipost.tistory.com/sitemap.xml`이 글 URL 2097개를 준다.
티스토리 글 주소는 제목이 슬러그로 박히므로 URL만으로 대학명·연도를 매칭할 수 있고,
실측에서 요청 1건·LLM 0콜로 시드 29곳 중 23곳의 2026 후보가 나왔다.

판단 시 유의할 점 셋:

- **이득은 커버리지가 아니라 비용이다.** 현재 실패하는 3곳(숙명·성신·동덕)은 사이트맵에도
  없다. 늘어나는 커버리지는 0곳이고, 줄어드는 것은 탐색 60초/건과 Claude 세션 한도 소모다
- **매칭은 지저분하다.** `서울대학교`에 `2026-서울대공원-장미원축제`가 걸렸다. 사이트맵은
  후보 생성기일 뿐이고 판정은 `verify`가 한다 — DEC-0017이 정한 구조 그대로다
- **출처 편중이 되돌아올 수 있다.** 단일 블로거 의존을 67%에서 낮춘 것이 2026-08-05의 성과다.
  사이트맵을 탐색 앞단에 두면 그 성과가 역행한다. 제안은 시드 → 사이트맵 → WebSearch 순으로
  두되 `discovery`에 `sitemap`을 기록해, 편중 정도를 시즌 후 측정하고 그때 순서를 재조정하는
  것이다 (파이프라인 문서 5.2의 피드백 루프)

# 한 대학의 봄·가을 축제를 모두 수집 — 설계

관련 이슈: #32

## 배경과 목표

대학 한 곳당 축제 한 건만 나온다. 시드가 대학당 한 행이고, 후보 중 검증을 통과한 첫
건에서 멈추며, 캐시가 대학 이름으로만 갈린다. 실제로는 봄과 가을을 모두 여는 대학이
있으므로 먼저 통과한 글 하나가 그 대학의 전부가 된다.

네 연도 산출물의 개최 월 분포가 이를 드러낸다.

| 연도 | 5월 | 9월 | 10월 | 날짜 없음 |
| --- | --- | --- | --- | --- |
| 2023 | 11 | 6 | 2 | 8 |
| 2024 | 12 | 4 | 3 | 9 |
| 2025 | 22 | 7 | — | — |
| 2026 | 24 | 1 | — | 2 |

시드는 네 해 모두 29행·고유 29곳이다. 2023년에 가을이 8곳인데 2026년에 1곳인 것은
대학이 가을 축제를 그만둔 것이 아니라 **어느 쪽이 남을지가 임의적**이기 때문이다.
봄이 남은 대학은 가을을, 가을이 남은 대학은 봄을 잃는다.

목표는 축제가 둘인 대학에서 둘 다 수집하는 것이다. 같은 축제를 두 번 내지 않는 것이
같은 무게의 목표다 — 중복은 지금 상태보다 나쁘다.

이 과제는 이미 알려져 있었다. `load_universities`의 중복 가드 주석과
DEC-0167(`import_key`에 개최 월을 더한 결정)이 "캐시 키를 캠퍼스·축제 단위로 바꾸는
후속 작업이 남아 있다"고 적어 두었다. DEC-0167의 계기 자체가 2025 서울대의 봄
(`Spring-kle`)과 가을(`SNUFESTIVAL: Scenario`)이 둘 다 후보로 잡혔는데 시드 1행이라
가을만 남은 사건이었다.

## 무엇이 잠그고 있는가

| 지점 | 현재 | 문제 |
| --- | --- | --- |
| `load_universities` | 같은 대학 2행이면 `SystemExit` | 시드에 둘째 행을 넣을 수 없다 |
| `raw/{university}.json` | 캐시 키가 대학 이름 | 두 축제가 같은 캐시를 덮어쓴다 |
| `discovered/{university}.json` | 후보 캐시가 대학 이름 | 두 행이 같은 후보를 받는다 |
| `_try_candidates` | 통과한 첫 건에서 `return True` | 둘째 축제를 찾을 기회가 없다 |
| `verify()` | 대학·연도만 대조 | **봄 글과 가을 글을 구분하지 못한다** |

마지막 항목이 핵심이다. 시드에 행을 더하고 캐시를 갈라도 verify가 구분자를 갖지
못하면 두 행이 똑같이 봄 글을 통과시켜 같은 축제가 두 키로 나간다.

## 설계

### 시드 스키마 — `season` 한 칸

```
university,campus,region,year,url,latitude,longitude,season
```

값은 `spring` | `fall`. **선택 컬럼이다.**

| 상황 | `season` | 수집 캐시 키 |
| --- | --- | --- |
| 축제 1개인 대학 | 비움 | `raw/{university}.json` (현행 유지) |
| 축제 2개인 대학 | 두 행 모두 채움 | `raw/{university}-{season}.json` |

선택으로 두는 이유는 캐시 무효화다. 전 행에 `season`을 강제하면 29곳 × 4개 연도의
캐시 키가 전부 바뀌어 116건 전체가 재수집된다. 비움을 허용하면 **손댄 대학만** 다시
돌고, 시드 마이그레이션도 필요 없다.

대신 느슨함을 막는 규칙을 둔다.

> 한 대학의 행 중 하나라도 `season`을 선언하면, 그 대학의 모든 행이 선언해야 한다.

선언하지 않은 행은 계절 제약이 없어 다른 계절의 글까지 가져간다. 위반은 기존 스타일
대로 `SystemExit`으로 크게 실패시킨다 — 조용히 통과하면 중복 축제가 만들어진다.

### 중복 가드

`load_universities`의 `seen`을 `university`에서 `(university, campus, season)`으로
바꾼다. 완전히 같은 조합이 두 번 나오면 그것은 진짜 중복이므로 `SystemExit`을 유지한다.
다캠퍼스 시드도 이 변경으로 함께 열린다.

### `verify()`의 계절 대조

```
SEASON_MONTHS = {"spring": (3, 6), "fall": (9, 11)}
```

추출된 `start_date`의 월이 선언한 계절 범위 밖이면 `mismatch`.

- `season`이 비어 있으면 계절을 보지 않는다 — 현행과 완전히 같다.
- `start_date`가 비어 있으면 통과시킨다. 모르는 것을 틀렸다고 판정하지 않는
  DEC-0138의 방침을 따른다.

남는 구멍을 명시한다: **날짜 없는 봄 글을 가을 행이 물어올 수 있다.** 사람이 검수에서
본다. 이 구멍을 막으려고 verify를 조이면 DEC-0138이 막으려던 일이 그대로 일어난다.

### 탐색

`discover`의 프롬프트에 계절을 실어 검색어를 가르고, 후보 캐시도
`discovered/{university}-{season}.json`으로 가른다. 이것을 빼면 가을 행의 후보 3개가
전부 봄 기사가 되어 verify가 셋 다 떨어뜨리고 `no_candidate`만 남는다.

`discover_sitemap`은 URL에 대학명과 연도가 들어 있는지만 본다. 계절은 URL에서 알 수
없으므로 필터를 더하지 않고 verify가 거른다.

### 추출 프롬프트에는 계절을 넣지 않는다

#30에서 고친 것이 정확히 이 구멍이다. 프롬프트가 알려준 값을 모델이 되돌려주고
검증이 그것을 확인하면 검증이 성립하지 않는다. "가을 축제"라고 알려주면 본문에 없는
가을 날짜를 지어낼 수 있다.

**추출은 본문만 읽고, 계절 판정은 추출이 끝난 뒤 verify가 한다.** DEC-0017의 역방향
검증 구조를 그대로 따른다.

### `import_key`

무변경. 이미 `{주최}-{캠퍼스}-{연도}-{월}`이라 5월 축제와 9월 축제가 자연히 다른 키를
받는다. DEC-0167이 이 상황을 위해 만든 형식이고 여기서 실제로 쓰인다.

한계: 두 축제 모두 `start_date`를 얻지 못하면 둘 다 `-00`이 되어 키가 충돌한다.
드문 경우이며 검수에서 잡는다.

### 검수 화면

`serve.py`의 `clear_cache`와 허용 목록이 대학 이름으로 행을 주소한다. 축제가 둘이면
「다시 돌리기」가 엉뚱한 캐시를 지운다. 같은 구분자를 받도록 최소 수정한다.

## 산출물 영향

`festivals.csv`에 그 대학의 행이 둘이 된다. 열 구성과 `lineup.csv`의 조인 방식은
바뀌지 않는다. 백엔드 임포트는 `import_key`가 다르면 다른 축제로 받으므로 별도 변경이
필요하지 않다.

## 테스트·검증

| 대상 | 확인 |
| --- | --- |
| `load_universities` | 같은 대학 2행 + 서로 다른 season → 통과 |
| `load_universities` | 같은 대학 2행 중 하나만 선언 → `SystemExit` |
| `load_universities` | 대학·캠퍼스·계절이 완전히 같은 2행 → `SystemExit` |
| 캐시 키 | season 없으면 기존 경로 그대로 (무효화 0을 증명한다) |
| 캐시 키 | season 있으면 수집·탐색 캐시가 갈린다 |
| `verify` | 가을 행이 5월 축제를 거른다 |
| `verify` | `start_date`가 없으면 통과시킨다 |
| `verify` | season이 없으면 현행과 동일하게 동작한다 |
| `discover` | 프롬프트에 계절이 실린다 |

실데이터 검증은 봄·가을이 모두 있는 대학 한 곳을 시드에 선언해 `--year`로 돌리고,
두 행이 서로 다른 축제로 나오는지 확인한다.

## 결정 요약

1. `season`은 선택 컬럼이다. 캐시 무효화를 피하기 위해서다.
2. 한 대학이 선언하면 그 대학의 모든 행이 선언한다. 위반은 크게 실패한다.
3. 계절 판정은 `start_date`의 월로 한다. 날짜가 없으면 통과시킨다.
4. 계절은 탐색 프롬프트에는 넣고 추출 프롬프트에는 넣지 않는다.
5. `import_key` 형식은 바꾸지 않는다.

## 범위 밖

- **탐색이 스스로 둘째 축제를 찾아내는 것.** 후보를 끝까지 태우면 추출 호출이 대학당
  최대 3배가 된다. 실측으로 호출 1회가 입력 약 104k 토큰이며, 전체 크롤이 현재
  27~33회다. 사람이 모르는 축제까지 잡는 이득보다 비용과 중복 위험이 크다고 보고
  이번에는 넣지 않는다.
- **시드에 가을 행을 실제로 채우는 일.** 이 설계는 넣을 수 있게 만들 뿐이고, 어느
  대학이 가을 축제를 여는지는 사람이 조사해 채운다.
- **`extract.py`의 서브프로세스 인코딩 문제.** 별건이며 별도 이슈로 다룬다.
- **게시일 경고의 검수 화면 표시.** 별건이며 별도 이슈로 다룬다.
